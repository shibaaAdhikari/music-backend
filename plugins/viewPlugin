import fastify from "fastify";
import fp from "fastify-plugin";
import fastifyView from "@fastify/view";
import ejs from "ejs";
export default fp(async function (fastify, optns) {
  fastify.register(fastifyView, {
    engine: {
      ejs: ejs,
    },
  });
});
